import {
    CSSProperties,
    ReactNode
} from "react";

interface IContainerProps {
    contentContainerClassName?: string;
    style?: CSSProperties;
    children?: ReactNode;
    className?: string;
};
export default IContainerProps;
